import SwiftUI

/// Root view — placeholder for Phase 3.
struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Lines")
                .font(.largeTitle)
                .navigationTitle("Lines")
        }
    }
}

#Preview {
    ContentView()
}

